ANTHROPIC_API_KEY=
DATABASE_URL=

# Optional: gates the whole app behind HTTP Basic Auth. Required once
# deployed publicly (see README) -- without these set, anyone with the URL
# could burn your Anthropic credits. Leave unset for local dev if you want.
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=
